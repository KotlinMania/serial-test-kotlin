# Port targets — `serial-test-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `serial_test` that codex-rs uses, and therefore the minimum subset that `serial-test-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-133] (rank `0`, tier `direct`, status `To Do`)

**Cargo package name**: `serial_test`
**Rust module name (in `use` statements)**: `serial_test`

## Summary

- **1** distinct symbols imported by codex-rs
- **12** total `use` statements
- **12** codex source files affected
- **5** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `serial_test::serial`

- **12 `use` site(s)** across **5 codex member crate(s)**: `codex-app-server`, `codex-core`, `codex-login`, `codex-rmcp-client`, `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server/src/message_processor_tracing_tests.rs:50` (`codex-app-server`) — `use serial_test::serial`
- `app-server/tests/suite/v2/account.rs:42` (`codex-app-server`) — `use serial_test::serial`
- `core/src/arc_monitor_tests.rs:6` (`codex-core`) — `use serial_test::serial`
- `core/tests/suite/rmcp_client.rs:63` (`codex-core`) — `use serial_test::serial`
- `core/tests/suite/tools.rs:41` (`codex-core`) — `use serial_test::serial`
- `login/src/auth/agent_identity.rs:82` (`codex-login`) — `use serial_test::serial`
- `login/src/auth/manager.rs:7` (`codex-login`) — `use serial_test::serial`
- `rmcp-client/src/auth_status.rs:201` (`codex-rmcp-client`) — `use serial_test::serial`
- `rmcp-client/src/utils.rs:152` (`codex-rmcp-client`) — `use serial_test::serial`
- `tui/src/chatwidget/tests.rs:179` (`codex-tui`) — `use serial_test::serial`
- `tui/src/external_editor.rs:97` (`codex-tui`) — `use serial_test::serial`
- `tui/src/lib.rs:1711` (`codex-tui`) — `use serial_test::serial`
